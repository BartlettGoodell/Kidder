name: Kidder Auto Publish

on:
  schedule:
    - cron: "0 */2 * * *"
  workflow_dispatch:
    inputs:
      rebuild:
        description: "Rebuild data.json even if no new emails (also prunes removed docx)"
        required: false
        default: "false"
        type: choice
        options:
          - "false"
          - "true"

permissions:
  contents: write

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install pandoc
        run: |
          sudo apt-get update
          sudo apt-get install -y pandoc

      - name: Install deps
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Ingest email attachments
        env:
          KIDDER_GMAIL_USER: ${{ secrets.KIDDER_GMAIL_USER }}
          KIDDER_GMAIL_APP_PASSWORD: ${{ secrets.KIDDER_GMAIL_APP_PASSWORD }}
        run: |
          CODE=0
          python scripts/email_ingest.py --folder KIDDER_ARTICLE_ARCHIVES || CODE=$?
          echo "INGEST_CODE=$CODE" >> "$GITHUB_ENV"

          # Acceptable:
          # 0 = no new emails
          # 8 = saved at least one attachment
          if [ "$CODE" -ne 0 ] && [ "$CODE" -ne 8 ]; then
            exit "$CODE"
          fi

      - name: Decide whether to process
        run: |
          SHOULD_PROCESS=0

          # If new email ingested, process
          if [ "${{ env.INGEST_CODE }}" = "8" ]; then
            SHOULD_PROCESS=8
          fi

          # If manual dispatch rebuild=true, process even without new emails
          if [ "${{ github.event_name }}" = "workflow_dispatch" ] && [ "${{ inputs.rebuild }}" = "true" ]; then
            SHOULD_PROCESS=8
          fi

          echo "SHOULD_PROCESS=$SHOULD_PROCESS" >> "$GITHUB_ENV"
          echo "Will process? $SHOULD_PROCESS"

      - name: Process articles (incremental + prune)
        if: env.SHOULD_PROCESS == '8'
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
        run: |
          CODE=0
          python scripts/process_articles.py \
            --folder KIDDER_ARTICLE_ARCHIVES \
            --output site/data.json \
            --resume --prune --verbose || CODE=$?
          echo "PROCESS_CODE=$CODE" >> "$GITHUB_ENV"

          # Acceptable:
          # 0 = nothing updated
          # 8 = updated JSON
          if [ "$CODE" -ne 0 ] && [ "$CODE" -ne 8 ]; then
            exit "$CODE"
          fi

      - name: Commit + push updated files
        if: env.PROCESS_CODE == '8'
        run: |
          git config user.name "kidder-bot"
          git config user.email "kidder-bot@users.noreply.github.com"
          git add site/data.json site/state.json missing_dates.csv KIDDER_ARTICLE_ARCHIVES || true
          git commit -m "Auto publish: update archive" || exit 0
          git push
